herramienta que nos ayude a simular un servidor

lo mandamos a llamar a traves de una url

En este caso node js

npm install -g json-server

npm = node package management (manejador de paquetes de node), cuando descargamos node ya viene por defecto

-g = para que se pueda instalar de manera global

json-server = nombre de la galeria


para verificar ponemos en el terminal json-server


--watch o -w


para usar json-server -w db.json



el contenido que tengamos en json-server se mostrará en la url  http://localhost:3000/perfil

para la extension en chrome poner google json formateer




AJAX= Asyncronous JavaScript and XML (JavaScript Asincrono y XML)

Nueva comunicacion entre en front end y backend

XML HTTP REQUEST => 

en lugar de que el navegador se encargue de obtener la informacion, haremos que js se encargue




como mantener un codigo estructurado, no anidar funciones sobre otra funcion

 

